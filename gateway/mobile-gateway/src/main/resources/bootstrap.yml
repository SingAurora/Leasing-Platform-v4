server:
  port: 8091
spring:
  application:
    name: leasing-mobile-gateway
  cloud:
    nacos:
      discovery:
        server-addr: fancyday.fit:8848
      config:
        server-addr: fancyday.fit:8848
        file-extension: yml
